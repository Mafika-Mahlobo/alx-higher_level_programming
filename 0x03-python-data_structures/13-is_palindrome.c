#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
* is_palindrome - checks if linked list is a palindrome
* @head: first pointer to first node of a linked list
* Return: Always 0
*/
int is_palindrome(listint_t **head)
{
	listint_t *temp = *head;

	if (temp == NULL)
		return (0)
	


	
	return (temp)
